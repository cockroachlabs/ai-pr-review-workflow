# Database Configuration
# Using CockroachDB dialect with psycopg
DATABASE_URL=cockroachdb+psycopg://root@localhost:26257/defaultdb

# API Configuration
API_TITLE=Full-Stack App API
API_VERSION=0.1.0

# GitHub API Token (Required)
# Used to fetch PR comments and code snippets for the dashboard
# Create a token at: https://github.com/settings/tokens
# Required scopes: repo (for private repos) or public_repo (for public repos only)
# IMPORTANT: Configure SSO for cockroachlabs and cockroachdb organizations
#            After creating token, click "Configure SSO" and authorize both orgs
# NOTE: This token should only be used on the backend because on the 
#       frontend, it can get baked into the build.
GITHUB_TOKEN=ghp_your_token_here
